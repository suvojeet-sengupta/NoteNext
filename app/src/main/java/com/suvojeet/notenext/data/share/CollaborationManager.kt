package com.suvojeet.notenext.data.share

/*
 * Real-time collaboration has been removed.
 *
 * Shared notes are now end-to-end encrypted, ephemeral secrets (see [ShareCrypto]):
 * the server stores only opaque ciphertext, so it can neither read nor relay live
 * edits. Shares are therefore read-only on the recipient's side. The old Socket.IO
 * CollaborationManager/CollabSession/NoteDelta types are gone; this file is left as
 * an intentional tombstone and can be deleted along with the unused io.socket
 * ("socket.io-client") Gradle dependency.
 */
